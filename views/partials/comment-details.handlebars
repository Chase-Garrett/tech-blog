<div class="card">
  <div class="card-body">
    <p class="card-text text-center">{{comment_text}}</p>
  </div>
  <div class="card-footer">
    <h6 class="text-end">
      Created by:
      {{user.user_name}}
      on:
      {{format_date createdAt}}</h6>
  </div>
</div>